# Upload de ficheiros (Assets)

O produto **Assets** (upload para Cloudflare R2, metadados em D1, rotas `/api/assets` e `/v1/assets`) foi **removido** do Tribus Hub. Não há fluxo de upload de ficheiros centralizado na aplicação web neste momento.
