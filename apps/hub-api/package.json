{
  "name": "@tribus/hub-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 execute tribus_hub_db --local --file=./migrations/0001_init.sql && wrangler d1 execute tribus_hub_db --local --file=./migrations/0002_seed_bootstrap_admin.sql",
    "d1:migrate:remote": "wrangler d1 execute tribus_hub_db --remote --file=./migrations/0001_init.sql && wrangler d1 execute tribus_hub_db --remote --file=./migrations/0002_seed_bootstrap_admin.sql"
  }
}
